import { css } from "styled-components";

const scrollX = css`
  overflow-x: scroll;
`;

const scrollY = css`
  overflow-y: scroll;
`;

const hiddenY = css`
  overflow-y: hidden;
`;
const hiddenX = css`
  overflow-x: hidden;
`;

export { scrollX, scrollY, hiddenY, hiddenX };
