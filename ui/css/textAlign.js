import { css } from "styled-components";

const textRight = css`
  text-align: right;
`;

const textLeft = css`
  text-align: left;
`;
const textCenter = css`
  text-align: center;
`;

export { textRight, textLeft, textCenter };
