import React from "react";
import styled, { css } from "styled-components";
import { textBase, textLg } from "../../css";

const subtitleCss = css`
  ${textBase};
  margin-bottom: 0.8rem;
`;
const Subtitle = styled.div`
  ${subtitleCss};
`;

export default Subtitle;
