import styled, { css } from "styled-components";

const titleCss = css`
  font-size: 1.2em;
  font-weight: 500;
`;

const Title = styled.div`
  ${titleCss};
`;

export { Title };
export default Title;
